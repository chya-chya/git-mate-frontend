# Changelog

Git-Mate 프론트엔드 프로젝트의 변경 이력 기록입니다. 모든 주요 업데이트와 버그 수정 사항은 이 문서에 누적되어 관리됩니다.

---

## [0.3.0] - 2026-06-08

### ✨ 추가된 기능 (Added)
* **ReportView 내 마크다운 링크 및 일반 URL 자동 앵커 태그 변환**
  * 리포트 뷰 텍스트 영역 내의 마크다운 형식 링크 및 일반 URL 텍스트를 동적으로 파싱하여 클릭 가능한 `<a>` 태그로 렌더링하도록 개선.
* **PR URL 링크 번호 요약 렌더링**
  * 가로로 길게 늘어지던 PR URL 전체 주소를 PR 번호 형태의 라벨 텍스트로 요약 및 결합하여 가독성 향상.

### 🐛 버그 수정 (Fixed)
* **대시보드 리포트 링크 경로 수정**
  * 대시보드 리포트 카드 컴포넌트 내 연결 경로가 `/analysis-reports/:id`로 잘못 지정되어 올바르게 렌더링되지 않던 버그를 정정하여 `/analysis/:id` 경로로 수정 및 정상 라우팅 확인.

### ♻️ 코드 개선 및 리팩토링 (Refactored)
* **프론트엔드 타입 정의 및 린트 경고 완전 해결**
  * 프론트엔드 소스코드 전체의 린트 에러 및 TypeScript 타입 체킹 에러를 분석하고 적합한 타입 인터페이스(`Interface`) 정의를 통해 빌드 안정성 확보.
* **네비게이션 바(Navbar) 렌더링 안정성 개선**
  * 네비게이션 바 컴포넌트 내의 렌더링 제어를 위해 무분별한 `eslint-disable` 주석을 제거하고, `requestAnimationFrame` 스케줄러를 활용해 우회하도록 리팩토링.
* **ReportView 헤더 동기화 시간 표기 간소화**
  * 분석 리포트 화면 헤더의 마지막 동기화 일시 출력 형식을 더 단순하고 가독성 좋게 포맷팅.
* **사이드바 메뉴 가독성 개선**
  * 사이드바 내 내비게이션 항목의 명칭을 명료하게 다듬어 전체 화면의 사용자 편의성 증대.

---

## [0.2.0] - 2026-06-03

### ✨ 추가된 기능 (Added)
* **나만의 개발 역량 분석카드 (구 소통명함) 팝업 및 관리**
  * 8대 역량 지표 중 가장 우수한 항목을 기반으로 동적 칭호와 시각 팝업 카드를 제공하는 모달 렌더링.
  * 생성된 분석카드를 고화질(3x pixel ratio) PNG 이미지 파일로 내려받는 캡처 다운로드 기능 추가.
  * SNS(X, LinkedIn)로 간편하게 자신의 개발 협업 분석 결과를 자랑할 수 있는 공유 링크 및 연동 숏컷 탑재.
* **대시보드 시각화 및 빠른 인지 카드 도입**
  * 메인 대시보드의 Recharts 레이더 차트에 고급스러운 그라데이션 필(Gradient Fill)과 네온 이펙트를 적용하여 첫인상 비주얼 상향.
  * 대시보드 최상단에 나의 가장 강력한 지표와 보완이 필요한 지표를 즉각 읽어내는 **'최대 강점'**, **'보완 필요 역량'** 요약 카드 신설.
* **체험형 인터랙티브 랜딩 페이지**
  * 비회원 방문자를 위해 랜딩 페이지 하단의 정적 리포트 목업을 사용자가 직접 지표 카드를 눌러보며 AI 코칭 변화를 확인하는 **인터랙티브 데모**로 전면 개편.
* **저장소 즐겨찾기(Pinning) 및 배지 최적화**
  * 자주 조회하고 동기화하는 저장소의 중요도 관리를 위한 **즐겨찾기(Pin) 고정 기능** 구현 (로컬 스토리지 연동).
  * 저장소 목록 내 다채로운 개발 언어 태그 배지와 분석 유도를 위한 버튼 마이크로 호버 애니메이션 고안.

### 🐛 버그 수정 (Fixed)
* **데이터 정합성 및 스케일 불일치 정정**
  * 백엔드 통계 데이터는 100점 만점으로 동작하지만 대시보드 화면상에서 `/ 5.0` 분모로 잘못 하드코딩 표기되고 있던 스코어 스케일 출력 오류를 `/ 100` 으로 완벽 정정.
* **Vercel 프로덕션 배포 컴파일 중단 해결**
  * `lucide-react` 라이브러리의 버전 의존성 및 네이밍 규칙 차이로 인해 Vercel CI/CD 환경에서 `Twitter`, `Linkedin` 아이콘 임포트 실패를 겪던 문제를 극복하기 위해, 해당 아이콘을 독자적인 고성능 **커스텀 인라인 SVG 컴포넌트**(`XIcon`, `LinkedinIcon`)로 완벽 교체 및 빌드 통과 성공.
* **저장소 카드 UI 오버플로우 교정**
  * 저장소 카드 핀 고정 시 '고정됨' 배지 노출 조건으로 인해 하단의 작동 버튼 그룹이 카드를 뚫고 아래로 튀어나와 뷰가 깨지던 CSS 레이아웃 틀어짐 버그 핫픽스 적용.

### ♻️ 코드 개선 및 리팩토링 (Refactored)
* **저장소 목록 가시성 극대화**
  * 불필요하고 혼선을 유발하던 스타 수(Star 0) 배지 및 부정확했던 가상 언어 스택 표기를 정리하여 미니멀하고 직관적인 디자인 체계 구축.
* **글로벌 내비게이션(GNB) 명료화**
  * 사이드바 및 네비게이션 메뉴명을 한글로 순화하여 서비스 접근 직관성 개선.
  * 8대 핵심 지표 선택 시 활성화 카드의 포커스 상태를 `Framer Motion`의 `layoutId` 무빙 하이라이터 인디케이터로 처리하여 부드러운 앱 감성의 전환 효과 구현.

---

*이 변경기록은 Keep a Changelog 표준 포맷을 준수하며, 깃 커밋 히스토리를 바탕으로 작성되었습니다.*
